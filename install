#!/bin/sh
# ruby
cd ruby
ruby install
gem install bundler
bundle config build.nokogiri --use-system-libraries
bundle install
cd ..
# python
pip install --upgrade pygments
# conf
conf=~/web/.conf
mkdir -p $conf
rsync -av config/ $conf
