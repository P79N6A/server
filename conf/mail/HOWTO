HOWTO: browse to email files in a browser

two paths will accumulate content:
address/  an index
msg/      permanent storage for message at Message-ID-derived location

to enable a hostname (for localhost, cd domain/localhost),

ln -s ../../address ../../msg .
