HOWTO: browse to email files in a browser

two paths will accumulate content:
address/  an index-tree of addresses
msg/      permanent location for messages

these paths aren't live until explicitly-linked to a host (for "localhost", cd domain/localhost)

ln -s ../../address ../../msg .
