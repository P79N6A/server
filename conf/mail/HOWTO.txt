HOWTO:

0) link rfc822-messages and index-trees to servable path
 $ ln -s ~/.mail domain/localhost
 $ ln -sr {address,msg} domain/localhost

1) browse email files over HTTP
GET http://localhost/today

N) cleanup
  $ rm -rf address msg index
