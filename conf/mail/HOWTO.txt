0) link messages and indexed-dir to servable path
 $ ln -s ~/.mail domain/mail
 $ ln -sr address domain/mail

1) browse email files over HTTP
 $ firefox mail/today

N) cleanup
  $ rm -rf address index
