GET     git clone https://github.com/hallwaykid/pw.git ||
        git clone http://repo.or.cz/w/www.git

PREINSTALL
 install ruby via distro-specific means
 Debian: apt-get install libssl-dev (for WebID user-certificates)

INSTALL cd pw/ruby
        bundle install # dependencies
	./install      # symlink source to ruby-path

USE     cd ..
        ln ruby/Procfile.thin Procfile # config foreman
        foreman start  # HTTP/HTTPS daemons on port 80/443
NOTE
- to listen on 80/443 (in lieu of >1024 behind apache/nginx/lighttpd)
  $ setcap cap_net_bind_service=+ep `which ruby`
- shell-binding to resource methods
  $ cp ruby/R ~/bin

HOST  mkdir domain/hostname
CLEAN rm -rf cache index
