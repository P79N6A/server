justArray -> splat
cascading-unglob the tree
add request-wide label-entries at R -> <a> conversion, this may requiire passing in the request context which should be magic too
gopher
nntp
ical
instagram
merge media and generic-element parse to single pass (Feed)
runtime-alternate storage locations for ingested docs
benchmark nokogiri/lax-SGML-parser feed parse vs regex
stats - doc view  - find largest mail, attachment 
change grep (view) arg-stacking to match fileset+model behavior (|| vs &&)
cache images
highlight and identify (for traversal) current timeseg-containers
ignore ?qs when guessing if URI might be an image
